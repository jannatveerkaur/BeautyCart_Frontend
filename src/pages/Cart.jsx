import { ArrowRight, Minus, Plus, Trash2 } from 'lucide-react'
import { Link } from 'react-router-dom'

function Cart({ cart, onRemove }) { const total = cart.reduce((sum, product) => sum + product.price, 0); return <section className="cart-page"><div className="page-heading compact"><p className="eyebrow">YOUR RITUAL</p><h1>Your bag.</h1></div>{cart.length === 0 ? <div className="empty-state"><div className="empty-mark">✦</div><h2>Your bag is taking a quiet moment.</h2><p>Discover something lovely for your next ritual.</p><Link className="primary-button" to="/shop">Explore the shop <ArrowRight size={15} /></Link></div> : <div className="cart-layout"><div className="cart-items">{cart.map((product) => <article className="cart-item" key={product.id}><img src={product.image} alt={product.name} /><div><p className="eyebrow">{product.category}</p><h3>{product.name}</h3><p>{product.size}</p><div className="quantity"><button type="button" aria-label="Decrease quantity"><Minus size={13} /></button><span>1</span><button type="button" aria-label="Increase quantity"><Plus size={13} /></button></div></div><strong>${product.price}</strong><button className="remove-button" type="button" aria-label={`Remove ${product.name}`} onClick={() => onRemove(product.id)}><Trash2 size={16} /></button></article>)}</div><aside className="order-summary"><p className="eyebrow">SUMMARY</p><h2>Ready when you are.</h2><div><span>Subtotal</span><strong>${total}</strong></div><div><span>Shipping</span><span className="muted">Calculated at checkout</span></div><hr /><div className="summary-total"><span>Total</span><strong>${total}</strong></div><button className="primary-button checkout-button" type="button">Continue to checkout <ArrowRight size={15} /></button></aside></div>}</section> }

export default Cart
